
const AssistantLayout = ({children}:{children:React.ReactNode}) => {

    return (
        <div className="bg-[#1B2041] w-full h-full flex items-center justify-center">
            {children}
        </div>
    )
}

export default AssistantLayout